import MySnippet from '/snippets/my-snippet.mdx';
import { ChainDisplay } from '/snippets/chain-display.jsx';

Gm World


<ParamField query="chainid" type="string" initialValue="1">
  The chainID to use
</ParamField>


<MySnippet word="bananas" />

The chain now is <ChainDisplay />

For details on building your own interactive components, see the [React guide](/essentials/react).
